# Embeddings-env requirements (.venv-emb): dense retrieval + bm25 methods.
# Kept separate from the generation env because sonar-space -> fairseq2 caps
# the torch version while vLLM floors it — the two stacks cannot coexist.
# Do NOT install guardians-mt-eval / sentinel here: unbabel-comet pins
# numpy<2 while fairseq2n needs numpy>=2.2. Sentinel lives in .venv-sent
# (see requirements-sent.txt); the embeddings SLURM job runs two passes.
numpy
torch
matplotlib
datasets>=2.18,<3.0
huggingface_hub
sonar-space
sentence-transformers
transformers
cohere
rank_bm25
python-dotenv
